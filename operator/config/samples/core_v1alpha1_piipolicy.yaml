apiVersion: core.pii-shield.io/v1alpha1
kind: PiiPolicy
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: piipolicy-sample
spec:
  # TODO(user): Add fields here
